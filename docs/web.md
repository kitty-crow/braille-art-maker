# Web app

The hero uses the bundled PNG and starts with colour, background colour and full colour enabled. Its monochrome/dither defaults remain Ordered 4x4 with inverted polarity.

The maker starts monochrome. Enabling Colour output reveals background and full-colour controls. TXT becomes tagged text, HTML contains inline foreground/background colours, and the preview uses the same `Art` cell colours without changing export dimensions.
